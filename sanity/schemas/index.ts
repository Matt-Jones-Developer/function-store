import store from './store-schema';
import product from './product-schema';
import page from './page-schema';
import user from './user-schema';

const schemas = [store, product, page, user];

export default schemas;
